/** @type {import('next').NextConfig} */
const nextConfig = {
    basePath: "/Website-Examples",
    output: "export",  // <=== enables static exports
    reactStrictMode: true,
  };
  
  module.exports = nextConfig;